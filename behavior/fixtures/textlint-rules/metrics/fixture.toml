id = "metrics-family"
tool = "textlint"
family = "metrics"

valid_prerequisites = [
  "textlint_package_built",
  "markdown_fixture_present"
]

intentionally_invalid = [
  "average sentence length above threshold",
  "coleman liau above threshold",
  "flesch reading ease below threshold",
  "gunning fog above threshold",
  "paragraph with seven sentences",
  "paragraph-local repeated word over threshold"
]

intentionally_valid = [
  "paragraph with six sentences",
  "short excluded word repeated many times",
  "component-like paragraph ignored"
]
