id = "term-policy-family"
tool = "textlint"
family = "term-policy"

valid_prerequisites = [
  "textlint_package_built",
  "markdown_fixture_present"
]

intentionally_invalid = [
  "missing required term",
  "recommended term pool below minimum"
]

intentionally_valid = [
  "one required term is present",
  "three recommended pool terms are present",
  "inflected recommended term matches rough stem"
]
