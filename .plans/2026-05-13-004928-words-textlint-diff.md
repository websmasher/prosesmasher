# Words Textlint Diff

## Counts

- hedge-stacking: Rust file failures 4, Rust evidence rows 5, textlint findings 5, textlint files 4
- simplicity: Rust file failures 7, Rust evidence rows 7, textlint findings 9, textlint files 7

## Hedge Rust Only (0)


## Hedge Textlint Only (0)


## Simplicity Rust-Only Files (0)


## Simplicity Textlint-Only Files (0)


## Simplicity Granularity

- Rust reports `simplicity` as file-level failures with no evidence rows for matched words.
- Textlint reports each matched complex word with source range, so it has 9 word findings across the same 7 files.