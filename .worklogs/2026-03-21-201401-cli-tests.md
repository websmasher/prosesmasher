# CLI adapter: bug fix + 24 tests, converged in 2 rounds

## Summary

Fixed exit code bug (main() always returned 0). Restructured CLI for testability: moved collect_files to lib, extracted format_line as pure function. Added 24 tests across args, checks, output, and file collection. Converged in round 2.

## Bug fixed

- BUG-01: main() now returns ExitCode::FAILURE on error (was always ExitCode::SUCCESS)

## Tests added

- args_tests.rs (5): clap parsing for file, config, group, missing path, no subcommand
- checks_tests.rs (8): all group counts, unknown/empty group errors, unique IDs, sum invariant
- output_tests.rs (5): PASS/FAIL formatting, label, observed value
- lib_tests.rs (5): single file, non-md passthrough, directory scanning, empty dir, sort order
