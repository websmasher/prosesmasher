# Plans 06-09: All remaining checks implemented

## Summary

Implemented 29 checks across 4 plans in parallel. 76 new tests. All 381 workspace tests pass, clippy clean.

## Plan 06 — Term checks (6 new): 29 tests
- banned_phrases (sliding-window phrase matching)
- gendered_terms, forbidden_terms, race_terms (same pattern as banned_words)
- hedge_stacking (per-sentence hedge count with unique column names)
- simplicity (complex→simple word pairs via BTreeSet)
- Shared `collect_paragraph_words` helper extracted to terms/mod.rs

## Plan 07 — Pattern checks (12 new): ~48 tests
- smart_quotes, exclamation_density, negation_reframe, triple_repeat
- fake_timestamps (English-only, character scanning for digit:digit AM/PM)
- colon_dramatic (short clause after colon, excludes lists)
- llm_openers, affirmation_closers, summative_closer, false_question
- humble_bragger, jargon_faker

## Plan 08 — Structure checks (7 new): 37 tests
- heading_hierarchy (H1 in body, H4+, level skips)
- heading_counts, bold_density, paragraph_length, sentence_case
- code_fences (flags any code block in prose)
- word_repetition (frequency map with stop word filtering)

## Plan 09 — Readability checks (4 new): 23 tests
- flesch_kincaid (206.835 - 1.015×(w/s) - 84.6×(syl/w))
- gunning_fog (0.4×((w/s) + 100×(complex/w)))
- coleman_liau (0.0588×L - 0.296×S - 15.8)
- avg_sentence_length (w/s)
- f64 scores converted to i64×100 for expectation API

## Key decisions
- All checks support all locales (None) except sentence_case (skips De/Ru) and fake_timestamps (En only)
- Per-sentence/paragraph checks use unique column names to avoid BTreeMap key collision in ExpectationSuite
- Readability checks skip silently when total_words or total_sentences is 0 (no div-by-zero)
- Empty config term lists cause check to skip (no expectation added)

## Next steps
- Plan 05: CLI adapter
- Test attacks on the 29 new checks
