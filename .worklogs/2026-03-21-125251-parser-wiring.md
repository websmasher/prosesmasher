# Plan 02 complete: MarkdownParser implements DocumentParser trait

## Summary

Wired `lib.rs` with `MarkdownParser` struct implementing the `DocumentParser` port trait. Plan 02 is now fully complete.

## Key files

- `crates/adapters/outbound/parser/src/lib.rs` — `MarkdownParser` struct, `DocumentParser` impl

## Next steps

- Plan 03: FS adapter (FsFileReader + FsConfigLoader with serde DTOs + garde validation)
- Plan 04: Check trait + runner + first 3 checks
